<h1><%= @author.name %></h1>
<h2>Publisher: <%= link_to @author.publisher.name, root_path %></h2>
<ul>
  <% @author.books.each do |book| %>
    <li><%= book.title %></li>
  <% end %>
</ul>
<%= link_to 'Destroy', author_path(@author), method: :delete %>
